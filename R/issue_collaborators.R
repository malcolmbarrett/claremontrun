#' Data about other collaborators on each issue, such as editors.
#'
#' `issue_collaborators` is a `tibble` that that contains data on other people
#' involved with each issue, inclding the editor-in-chief, the editor, and the
#' penciller.
#'
#' @format A `tibble` with 190 rows and 4 variables: `issue`, the issue number,
#'   `editor_in_chief`, `editor`, and `penciller`.
#'
"issue_collaborators"
